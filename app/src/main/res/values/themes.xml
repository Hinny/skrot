<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; the XML theme just provides a dark, action-bar-less shell. -->
    <style name="Theme.Skrot" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/skrot_background</item>
    </style>
</resources>
